version: "3.1"

services:
  database:
    image: "mongo:latest"
    container_name: "mongodb"
    ports:
      - 27017:27017
    environment:
      - MONGO_INITDB_ROOT_USERNAME=alice
      - MONGO_INITDB_ROOT_PASSWORD=Alice.123
      - MONGO_INITDB_DATABASE=alice